apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orphaned-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: "nonexistent-storage-class"