apiVersion: v1
kind: Pod
metadata:
  name: memory-pressure-test
spec:
  containers:
  - name: memory-consumer
    image: progrium/stress
    args: ["--vm", "1", "--vm-bytes", "1G"]
    resources:
      limits:
        memory: "1Gi"