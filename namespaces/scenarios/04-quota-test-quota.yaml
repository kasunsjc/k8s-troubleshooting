apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota-test-quota
  namespace: quota-test
spec:
  hard:
    requests.cpu: "100m"
    requests.memory: "128Mi"
    limits.cpu: "200m"
    limits.memory: "256Mi"
    pods: "2"